<h1> Hello, <%= @user_name %>! </h1>

<h2> We'll also add new friends for <%= @user_name %>, so they can enjoy their new home! :) </h2>

<img src="<%= @image_url %>" alt="A <%= @user_choice %>">

<div>
  <p>
    Click the paw to add a new friend! 
  </p>
  <a href="/">
    <i class="fa-solid fa-paw"></i>
  </a>
</div>
